npx for execute node.js packaged binaries
> npx hexy --width 12 ls.js

Feature check
> http://node.green/

Babel
```
npm install babel
./node_modules/.bin/babel app.js -o new-app.js
```

yield = await